To build X-VR2 libraries, go to libs
Quick build:
	./recompile-all.sh
Long build:
	move to each subdirectory, then
	1. ./autogen.sh
	2. ./configure --help 
	3. ./configure --[USE ANY PARAMETERS as given by the previous command]
	4. make
	5. make install
