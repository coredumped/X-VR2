This package holds some basic demos which must be built with xvr2core
The same steps as allways
1. configure
2. make
3. make install (though you might not wanna install the binaries after all right?)

Just explore each subdirectory to run the binaries.


Have fun!!
